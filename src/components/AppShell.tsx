import { Link, useLocation, useNavigate } from "@tanstack/react-router";
import { Bell, Home, Menu, ShieldCheck, UserRound, X } from "lucide-react";
import { useState, type ReactNode } from "react";

export interface AppShellProps { area: "admin" | "u"; title: string; children: ReactNode; back?: string; }

export function AppShell({ area, title, children, back }: AppShellProps) {
  const [open, setOpen] = useState(false);
  const [areaOpen, setAreaOpen] = useState(false);
  const location = useLocation();
  const navigate = useNavigate();
  const goToArea = (path: "/admin" | "/u") => { setAreaOpen(false); navigate({ to: path }); };

  return <div className="min-h-screen bg-background"><header className="sticky top-0 z-40 border-b border-border bg-surface/80 backdrop-blur"><div className="mx-auto flex max-w-5xl items-center justify-between px-3 py-3"><div className="flex items-center gap-3"><div className="relative"><button onClick={() => setAreaOpen((value) => !value)} aria-label="Scegli area Admin o Utente" className="inline-flex h-9 w-9 items-center justify-center rounded-md border border-border bg-surface text-foreground hover:bg-muted"><Home className="h-5 w-5" strokeWidth={1.5} /></button>{areaOpen && <div className="absolute left-0 top-11 z-50 w-52 border border-border bg-surface p-2 shadow-lg"><p className="px-2 pb-2 text-xs font-semibold uppercase tracking-[0.08em] text-muted-foreground">Apri area</p><button onClick={() => goToArea("/admin")} className="flex w-full items-center gap-2 px-3 py-2 text-left text-sm text-foreground hover:bg-muted"><ShieldCheck className="h-4 w-4 text-accent" /> Admin</button><button onClick={() => goToArea("/u")} className="flex w-full items-center gap-2 px-3 py-2 text-left text-sm text-foreground hover:bg-muted"><UserRound className="h-4 w-4 text-accent" /> Utente</button></div>}</div>{back && <Link to={back} aria-label="Indietro" className="inline-flex h-9 w-9 items-center justify-center rounded-md border border-border bg-surface text-foreground hover:bg-muted">←</Link>}<div><p className="eyebrow text-xs text-accent">{area === "admin" ? "Ufficio" : "Area personale"}</p><h1 className="font-serif text-xl text-primary">{title}</h1></div></div><button onClick={() => setOpen((value) => !value)} className="inline-flex h-9 w-9 items-center justify-center rounded-md border border-border bg-surface text-foreground hover:bg-muted md:hidden" aria-label="Menu">{open ? <X className="h-5 w-5" /> : <Menu className="h-5 w-5" />}</button></div></header><nav className={`mx-auto max-w-5xl px-3 py-2 ${open ? "block" : "hidden md:block"}`}><ul className="flex flex-wrap items-center gap-2"><li><Link to="/admin" className={`rounded-md px-3 py-2 text-sm font-semibold ${location.pathname.startsWith("/admin") ? "bg-primary text-white" : "text-foreground hover:bg-muted"}`}>Admin</Link></li><li><Link to="/u" className={`rounded-md px-3 py-2 text-sm font-semibold ${location.pathname.startsWith("/u") ? "bg-primary text-white" : "text-foreground hover:bg-muted"}`}>Utente</Link></li></ul></nav><main className="mx-auto max-w-5xl pb-24 pt-4">{children}</main><footer className="fixed bottom-0 left-0 right-0 border-t border-border bg-surface/95 backdrop-blur md:hidden"><div className="mx-auto flex max-w-5xl items-center justify-around px-3 py-2"><button onClick={() => setAreaOpen((value) => !value)} className="flex flex-col items-center gap-1 px-3 py-1 text-muted-foreground"><Home className="h-5 w-5" strokeWidth={1.5} /><span className="text-[10px] font-semibold uppercase tracking-[0.08em]">Aree</span></button><Link to="/admin" className={`flex flex-col items-center gap-1 px-3 py-1 ${location.pathname.startsWith("/admin") ? "text-primary" : "text-muted-foreground"}`}><ShieldCheck className="h-5 w-5" strokeWidth={1.5} /><span className="text-[10px] font-semibold uppercase tracking-[0.08em]">Admin</span></Link><Link to="/u" className={`flex flex-col items-center gap-1 px-3 py-1 ${location.pathname.startsWith("/u") ? "text-primary" : "text-muted-foreground"}`}><Bell className="h-5 w-5" strokeWidth={1.5} /><span className="text-[10px] font-semibold uppercase tracking-[0.08em]">Utente</span></Link></div></footer></div>;
}
